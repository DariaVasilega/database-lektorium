{% extends 'base.html.twig' %}

{% block title %}ProjectPeople{% endblock %}

{% block body %}
    <h1>ProjectPeople</h1>

    <table class="table table-sm table-dark">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ project_person.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ project_person.Type }}</td>
            </tr>
            <tr>
                <th>Responsibility</th>
                <td>{{ project_person.Responsibility }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-dark" href="{{ path('project_people_index') }}" role="button">Back to list</a>

    <a href="{{ path('project_people_edit', {'id': project_person.id}) }}">edit</a>
    <a class="btn btn-dark" href="{{ path('project_people_edit', {'id': project_person.id}) }}" role="button">edit</a>

    {{ include('project_people/_delete_form.html.twig') }}
{% endblock %}
